# Produktions-Env für den offiziellen Go-live auf IONOS (www.dirkmathesius.de).
# Wird von `npm run build:ionos` (vite build --mode ionos) über .env gelegt.
# Nur client-seitig sichtbare Werte (kein echtes Secret) → im Repo ok.
# Das SFTP-Passwort liegt NICHT hier, sondern in ~/.dirkmathesius-ionos-ftp.env (chmod 600).

VITE_SITE_URL=https://www.dirkmathesius.de
VITE_SITE_VARIANT=official
VITE_GA4_ID=G-NHPNTGY90D
VITE_GOOGLE_ADS_ID=
VITE_WEB3FORMS_KEY=dd80ab94-365d-4abf-96d3-6c915369011b
